# Local .terraform directories
**/.terraform/*

# .tfstate files (NEVER commit these!)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files (Likely contain secrets/passwords)
*.tfvars
*.tfvars.json

# Ignore override files used for local testing
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraform.lock.hcl file
# (Keep this commented out! You usually WANT to commit the lock file to ensure versions match)
# .terraform.lock.hcl